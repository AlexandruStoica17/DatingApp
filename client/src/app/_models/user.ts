export interface User{ //nu e ca in C# sa punem iUser pt interfata
    username: string; //daca lasam username? : string username poate fi definit ca string sau undefined
    // (adica proprietatile sunt optionale)
    token: string;
}